{
  "name": "complianceloop-site",
  "private": true,
  "scripts": {
  "prebuild": "rm -rf public && mkdir -p public && cp -R complianceloop-starter/* public/",
  "build": "npm run prebuild && next build",
  "start": "next start"
},
  "dependencies": {
    "next": "^14.2.5",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
